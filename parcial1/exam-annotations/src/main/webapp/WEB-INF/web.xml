<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
         https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>examen-iot-annotations</display-name>

    <!-- Le decimos a Spring que el contexto se arma con una clase @Configuration,
         no con un XML. Por eso cambiamos el contextClass. -->
    <context-param>
        <param-name>contextClass</param-name>
        <param-value>org.springframework.web.context.support.AnnotationConfigWebApplicationContext</param-value>
    </context-param>
    <context-param>
        <param-name>contextConfigLocation</param-name>
        <param-value>com.exam.iot.config.AppConfig</param-value>
    </context-param>

    <!-- Este listener crea el ApplicationContext al arrancar la app y lo
         guarda en el ServletContext, para que persista entre solicitudes. -->
    <listener>
        <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
    </listener>

</web-app>
